machine:
  services:
    - docker
  java:
    version: oraclejdk8

dependencies:
  pre:
    - ./gradlew -q dependencies

general:
  branches:
    only:
      - master
      - develop

test:
  post:
    - ./gradlew -q distDocker

